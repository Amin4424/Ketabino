# گزارش تقسیم کار پروژه کتابینو (Ketabino)

### علی شیدایی (شماره دانشجویی: 402442234)
* **بخش بک‌اند (۱۰ موجودیت)**:
  * پیاده‌سازی و مدیریت پایگاه داده بخش شبکه‌های اجتماعی، تعاملات و حلقه‌های همخوانی شامل جدول‌های:
    1. `BOOK_CLUBS` (حلقه‌های همخوانی)
    2. `BOOK_CLUB_MEMBERS` (اعضای حلقه‌ها)
    3. `BOOK_CLUB_MESSAGES` (پیام‌های حلقه‌ها)
    4. `REVIEWS` (نظرات و امتیازدهی)
    5. `COMMENTS` (دیدگاه‌ها)
    6. `LIKES` (پسندیدن کتاب‌ها)
    7. `BOOKMARKS` (نشانه‌گذاری‌های مطالعه)
    8. `HIGHLIGHTS` (هایلایت‌های متنی)
    9. `NOTIFICATIONS` (اعلان‌ها)
    10. `REPORTS` (گزارش تخلفات)
  * پیاده‌سازی کنترلرها و منطق بیزینس مربوطه (`BookClubController`، `ReportController` و متدهای تعاملی در `BookController`).
* **بخش فرانت‌اند**:
  * طراحی و پیاده‌سازی افکت‌های حرکتی، ترنزیشن صفحات و انیمیشن‌های پویا با استفاده از کتابخانه `framer-motion` (نظیر انیمیشن چت حلقه‌ها، کارت‌های چالش و افکت هدر).

---

### محمد امین شمسی (شماره دانشجویی: 402442225)
* **بخش بک‌اند (۱۰ موجودیت)**:
  * پیاده‌سازی و مدیریت پایگاه داده بخش هسته سیستم، کاربری، مالی و چالش‌ها شامل جدول‌های:
    1. `USERS` (کاربران)
    2. `USER_SUBSCRIPTIONS` (اشتراک کاربران)
    3. `SUBSCRIPTION_PLANS` (طرح‌های اشتراک)
    4. `WALLETS` (کیف پول)
    5. `TRANSACTIONS` (تراکنش‌های مالی)
    6. `COIN_PACKAGES` (پکیج‌های خرید سکه)
    7. `BOOKS` (کتاب‌ها)
    8. `CHAPTERS` (فصل‌های کتاب)
    9. `CHALLENGES` (چالش‌های مطالعه)
    10. `USER_CHALLENGES` (چالش‌های کاربر)
  * پیاده‌سازی مکانیزم احراز هویت توکن JWT، منطق تراکنش‌های امن مالی و شارژ سکه، و راه‌انداز خودکار پایگاه داده (`SchemaInitializer`).
* **بخش فرانت‌اند**:
  * طراحی و پیاده‌سازی ساختار و اسکافلد اصلی پروژه (Scaffolding)، معماری پوشه‌ها در Next.js App Router، کلاس کلاینت API، قالب‌های کلی صفحات (Layouts)، گاردها و صفحات ورود و پروفایل کاربری.
