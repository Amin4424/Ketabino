# مستندات و گزارش معماری بخش بک‌اند پروژه کتابینو (Ketabino)

پروژه کتابینو یک پلتفرم مطالعه و انتشار کتاب دیجیتال است. بخش بک‌اند این پروژه با استفاده از **ASP.NET Core Web API** طراحی و پیاده‌سازی شده و از پایگاه داده سبک و سریع **SQLite** بهره می‌برد. در ادامه، معماری، ساختار داده‌ها، جدول‌های پایگاه داده و بخش‌های مختلف سرویس بک‌اند به تفصیل تشریح شده است.

---

## ۱. معماری و ساختار پروژه

بخش بک‌اند از ساختار معماری کنترلر-مدل (MVC/API) پیروی می‌کند و بخش‌های اصلی آن عبارتند از:

* **Controllers**: کلاس‌های دریافت‌کننده درخواست‌های HTTP که وظیفه اعتبارسنجی ورودی‌ها، فراخوانی سرویس‌ها یا تغییرات مستقیم در پایگاه داده و در نهایت تولید پاسخ‌های مناسب (JSON) را بر عهده دارند.
* **Models**: تعریف ساختارهای داده ورودی (Request DTOs)، خروجی (Response DTOs) و موجودیت‌های اصلی برنامه.
* **Database**: شامل کلاس‌های ارتباط با پایگاه داده نظیر `SqliteDbHelper` برای اجرای آسان و امن دستورات SQL پارامتریزه شده، و کلاس `SchemaInitializer` جهت راه‌اندازی و تزریق داده‌های اولیه.
* **Services**: پیاده‌سازی منطق‌های کسب‌وکار خاص (مانند اعتبارسنجی امنیتی).

---

## ۲. معماری پایگاه داده (۲۶ موجودیت فعال)

پایگاه داده پروژه شامل **۲۶ جدول** است که روابط چندبه‌چند، ردیابی فعالیت‌ها، سیستم مالی، سیستم همخوانی و چالش‌های مطالعه را مدیریت می‌کنند. ساختار جدول‌ها به شرح زیر است:

### موجودیت‌های پایه و کاربری
1. **`USERS`**: نگهداری اطلاعات کاربران شامل نام، شماره تلفن، نقش (Admin / Author / Reader) و زمان ثبت‌نام.
2. **`USER_SUBSCRIPTIONS`**: ردیابی اشتراک‌های فعال خریداری‌شده توسط خوانندگان.
3. **`SUBSCRIPTION_PLANS`**: تعریف پکیج‌های اشتراک زمانی (مثلا ۳۰ روزه) به همراه قیمت و توضیحات.

### موجودیت‌های مالی و کیف پول
4. **`WALLETS`**: نگهداری موجودی سکه (Coin Balance) کاربران.
5. **`TRANSACTIONS`**: ثبت تاریخچه تمام واریزها، برداشت‌ها و خریدهای کاربران جهت شفافیت مالی.
6. **`COIN_PACKAGES`**: تعریف پکیج‌های خرید سکه با پول واقعی برای افزایش موجودی کیف پول.

### موجودیت‌های کتاب و محتوا
7. **`BOOKS`**: ذخیره اطلاعات اصلی کتاب شامل عنوان، نویسنده، ژانرها، وضعیت انتشار و تصویر جلد.
8. **`BOOK_GENRES`**: جدول واسط چندبه‌چند میان کتاب‌ها و ژانرها.
9. **`GENRES`**: دسته‌بندی موضوعی کتاب‌ها (داستانی، علمی، رمان و...).
10. **`CHAPTERS`**: نگهداری فصل‌های کتاب شامل متن اصلی، قیمت فصل (رایگان یا سکه‌ای) و وضعیت انتشار.
11. **`USER_CHAPTERS`**: ردیابی فصل‌های خریداری‌شده توسط کاربران خواننده.

### سیستم تعاملات و شبکه‌های اجتماعی
12. **`LIKES`**: ثبت کتاب‌های پسندیده‌شده توسط کاربران.
13. **`BOOKMARKS`**: ذخیره موقعیت آخرین مطالعه کاربر در هر فصل به همراه یادداشت‌های شخصی.
14. **`HIGHLIGHTS`**: نشانه‌گذاری بخش‌های خاصی از متن کتاب با رنگ‌های مختلف و ذخیره آن.
15. **`REVIEWS`**: ثبت امتیاز (۱ تا ۵) و نظر متنی کاربران برای کتاب‌ها (با محدودیت یک نظر فعال برای هر کاربر روی هر کتاب که قابلیت به‌روزرسانی دارد).
16. **`COMMENTS`**: دیدگاه‌های کاربران روی کتاب‌ها با ساختار درختی (پشتیبانی از پاسخ یا Reply به نظرات دیگران).

### موجودیت‌های مدیریت و نظارت (Moderation)
17. **`REPORTS`**: گزارش تخلفات ارسال شده توسط کاربران برای کتاب‌ها، فصل‌ها یا نظرات جهت بررسی ادمین.
18. **`NOTIFICATIONS`**: اعلان‌های ارسالی به کاربران (مانند تایید خرید، اطلاعیه‌های نویسندگان یا تخلفات).
19. **`SYSTEM_LOGS`**: ثبت رویدادهای سیستمی و ادمین برای پایش رفتار نرم‌افزار.

### سیستم حلقه‌های کتابخوانی (جدید)
20. **`BOOK_CLUBS`**: حلقه‌های همخوانی کتاب که توسط نویسندگان یا کاربران ایجاد می‌شوند.
21. **`BOOK_CLUB_MEMBERS`**: ثبت کاربران عضو در هر حلقه کتابخوانی.
22. **`BOOK_CLUB_MESSAGES`**: گفتگوهای متنی و پیام‌های تبادل‌شده در اتاق گفتگوی حلقه.

### سیستم چالش‌های مطالعه (جدید)
23. **`CHALLENGES`**: تعریف چالش‌های مطالعه دوره‌ای (مانند مطالعه ۵ کتاب در ماه) به همراه جوایز سکه‌ای.
24. **`USER_CHALLENGES`**: ردیابی پیشرفت انفرادی، درصد تکمیل و ادعای پاداش چالش توسط کاربر.

---

## ۳. کنترلرها و نقاط دسترسی API

بک‌اند مجموعه کاملی از نقاط دسترسی (RESTful Endpoints) را ارائه می‌دهد که همگی به مکانیزم امنیتی JWT مجهز هستند:

### ۱. احراز هویت (`AuthController.cs`)
* `POST /api/auth/register`: ثبت‌نام کاربر جدید به همراه ایجاد خودکار کیف پول.
* `POST /api/auth/login`: ورود با شماره تلفن همراه و دریافت توکن دسترسی JWT.

### ۲. مدیریت کتاب‌ها و ژانرها (`BookController.cs` & `GenreController.cs`)
* `GET /api/book`: لیست کتاب‌های منتشر شده با قابلیت جستجو و فیلتر بر اساس ژانر.
* `GET /api/book/{id}`: دریافت اطلاعات تفصیلی یک کتاب به همراه آمار تعاملات.
* `POST /api/book/{id}/like` / `DELETE`: مدیریت علاقه‌مندی‌ها.
* `POST /api/book/{id}/review`: ثبت یا ویرایش نظر و امتیاز به صورت UPSERT (جلوگیری از ایجاد خطای تکرار).

### ۳. مدیریت فصل‌ها و محتوا (`ChapterController.cs`)
* `GET /api/chapter/book/{bookId}`: دریافت لیست فصل‌های یک کتاب.
* `GET /api/chapter/{id}`: دریافت متن فصل (بررسی قفل بودن و خرید با سکه).
* `POST /api/chapter/{id}/purchase`: خرید دسترسی به فصل‌های پولی با کسر موجودی کیف پول.

### ۴. کیف پول و مالی (`WalletController.cs`)
* `GET /api/wallet/balance`: دریافت موجودی فعلی.
* `POST /api/wallet/deposit`: شارژ آزمایشی حساب کاربری.
* `GET /api/wallet/transactions`: دریافت تاریخچه کامل تراکنش‌ها.

### ۵. اشتراک‌ها (`SubscriptionController.cs`)
* `GET /api/subscription/plans`: دریافت لیست طرح‌های اشتراک فعال.
* `GET /api/subscription/active`: بررسی وضعیت اشتراک فعلی کاربر.
* `POST /api/subscription/purchase`: خرید اشتراک با کسر هزینه از کیف پول و ثبت زمان پایان اشتراک.

### ۶. چالش‌ها و حلقه‌ها (`ChallengeController.cs` & `BookClubController.cs`)
* `GET /api/challenge`: لیست چالش‌ها و درصد پیشرفت کاربر.
* `POST /api/challenge/{id}/progress`: شبیه‌سازی و افزایش پیشرفت مطالعه در چالش.
* `POST /api/challenge/{id}/claim`: واریز پاداش سکه‌ای چالش تکمیل شده به کیف پول کاربر در یک تراکنش امن بانک اطلاعاتی.
* `POST /api/bookclub`: ساخت حلقه همخوانی جدید.
* `POST /api/bookclub/{id}/message`: ارسال پیام به چت‌روم حلقه.

---

## ۴. امنیت و مدیریت پایگاه داده

1. **مکانیزم احراز هویت (JWT)**: ورود امن با صدور توکن‌های منقضی‌شونده انجام شده و دسترسی به متدهای مهم نظیر پنل مدیریت ادمین یا استودیوی نویسنده بر اساس نقش‌های (`Admin` / `Author`) بررسی و محدود می‌گردد.
2. **پایگاه داده تراکنشی**: در بخش‌های حساسی چون «خرید اشتراک»، «خرید فصل کتاب» و «دریافت پاداش چالش»، تمام کوئری‌ها در قالب یک تراکنش یکپارچه (`SqliteTransaction`) اجرا می‌شوند تا در صورت وقوع هرگونه خطای سیستمی، عملیات به وضعیت قبل برگشت داده شده (Rollback) و از ناهماهنگی اطلاعات یا کسر نامعتبر پول جلوگیری شود.
3. **مکانیزم راه‌انداز خودکار (Seeding)**: در کلاس `SchemaInitializer.cs` تعاریف جدول‌ها نگهداری شده و در صورت نبود بانک اطلاعاتی، به‌طور خودکار فایل `ketabino.db` ایجاد، جدول‌ها ساخته شده و داده‌های اولیه و اکانت‌های تستی (مانند ادمین، نویسنده و خواننده) تزریق می‌شوند.
